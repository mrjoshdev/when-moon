<h1></h1>
<ul class="cryptocurrency-feed large-12">

</ul>

<p>
  <%= image_tag("Rocket_Image.png", :class=>"rocket") %>
  <link href='https://fonts.googleapis.com/css?family=Lato:300,400,700' rel='stylesheet' type='text/css'>
  <div id='stars'></div>
  <div id='stars2'></div>
  <div id='stars3'></div>
  <div id='title'>
    <span>
      WHEN MOON?
    </span>
  <%= image_tag("Super_Moon.png", :class=>"moon") %>
    <br>
    <span>
    </span>
  </div>
</p>

<button id="button" type="button" name="button"> <%= link_to "Add Cryptocurrency", new_cryptocurrency_path %> </button>
<% if current_user %>
  <button id="button" type="button" name="button"> <%= link_to "Edit Cryptocurrency", "/users/#{current_user.id}"%> </button>
<% end %>
<br>
<br>
<br>
<br>

<dl>
  <dt>
    Progress to Moon
  </dt>
  <dd class="percentage percentage-11"><span class="text-css-chart">BTC: 11.33%</span></dd>
  <dd class="percentage percentage-49"><span class="text-css-chart">ETH: 49.77%</span></dd>
  <dd class="percentage percentage-16"><span class="text-css-chart">ETC: 16.09%</span></dd>
  <dd class="percentage percentage-5"><span class="text-css-chart">LTC: 5.41%</span></dd>
  <dd class="percentage percentage-2"><span class="text-css-chart">EOS: 1.62%</span></dd>
  <dd class="percentage percentage-2"><span class="text-css-chart">DOGE: 2%</span></dd>

<div class="crypto_list">
<!--   Cryptocurrencies Database <br>
<!--  <% num = 0 %>
  <% @cryptocurrencies.each do |coin1| %>
  <% num += 1 %>
    Coin: <%= coin1["name"] %>
    <br>
    Symbol: $<%= coin1["symbol"] %>
    <br>
  <br>
  <% end %> -->
<p>
  <span id="first_name">
  <% if current_user %>
    <%= current_user.first_name %>'s favorite cryptocurrenices
  <% end %>
  </span>
</p>
  <% num = 0 %>
  <% @coins["data"].each do |coin| %>
    <% num += 1 %>
    <% num.to_s %>
    <% if current_user %>
      <% @user_cryptocurrencies.each do |cc| %>

        <% if current_user['id'] && cc.symbol == coin[1]["symbol"] %>
        <%= target = cc.target_price %>
        <%= current_price = coin[1]["quotes"]["USD"]["price"] %> <br>
        <%= progress = (current_price / target * 100).round %>

          Coin: <%= coin[1]["symbol"] %>
          <br>
          <%=  %>
          Current Price: $<%= '%.3f' % coin[1]["quotes"]["USD"]["price"] %>
          <%= progress_bar = "percentage percentage-#{progress}" %>
          <dd class=<%=progress_bar%>><span class="text-css-chart"><%= coin[1]["symbol"] %></span></dd>
          <br>
        <br>
        <% end %>
      <% end %>
    <% end %>
  <% end %>

</div>

</dl>
